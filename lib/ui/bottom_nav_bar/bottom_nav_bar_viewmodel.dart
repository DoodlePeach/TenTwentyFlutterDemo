import 'package:stacked/stacked.dart';

// Navigation does not seem to play a role in the demo app.
class BottomNavBarViewModel extends BaseViewModel {
  // final _dialogService = locator<NavigationService>();
}
